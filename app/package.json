{
  "name": "ambery-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "pretest": "cargo build -p ambery-case --manifest-path ../Cargo.toml",
    "test": "..\\target\\debug\\ambery-case.exe frontend --silent"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4",
    "@types/ws": "^8.18.1",
    "jsdom": "^30.0.1",
    "typescript": "^7.0.2",
    "vite": "^8.1.5",
    "vitest": "^4.1.10",
    "ws": "^8.21.2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.11.1"
  }
}
