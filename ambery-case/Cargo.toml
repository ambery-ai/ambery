[package]
name = "ambery-case"
version = "0.1.0-alpha0"
edition = "2021"

[dependencies]
ambery-core = { path = "../core", features = ["case-runner", "debug-agent"] }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
