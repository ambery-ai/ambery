[package]
name = "overseer-case"
version = "0.1.0"
edition = "2021"

[dependencies]
overseer-core = { path = "../core", features = ["case-runner", "mock"] }
serde_json = "1"
serde = { version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
